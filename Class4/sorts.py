# selection sort => finds local min and places in the first index and so on... O(n^2)
# insertion sort => Order hand of cards approach O(n) to O(n^2) (best and worst case)
# mergesort => divide list into smaller sublist (challenge)
